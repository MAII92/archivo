using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Reflection;
using System.Text;
using System.Threading.Tasks;

namespace ARCHIVO
{
    class FileManager
    {
        public static void SaveFile(string fileName, string content)
        {

            String SaveFile = fileName + ".txt";


            using (StreamWriter sw = new StreamWriter(SaveFile, true))
            {



                 String  content = content.tipoRegistro + valor + referenciaPrincipal + referenciaSecundaria + periodosFacturas + ciclo + valorPrincipal + codigoServicio + valorAdicional + fechaVencimiento
                FileManager.SaveFile(fileName, content);





                FileManager.SaveFile(content);
            }
        }
        
    }
}

